const wallHeight = 80;

function calculate (value, value2) {

  return value * value2;
}
const width = calculate(100, 200);
const height = calculate(wallHeight)  //80

console.log(width, height)

const dimensions = [width, height]
console.log(dimensions)